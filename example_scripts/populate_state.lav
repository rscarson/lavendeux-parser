// You can register functions and variables
factorial(x) = x==0 ? 1 : (x * factorial(x - 1) )
important_value = 5

// Or call functions to set up the parser
api_register("animechan", "https://animechan.vercel.app/api/random")